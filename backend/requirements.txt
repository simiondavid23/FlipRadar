# FlipRadar - dependinte backend
# Python 3.11+ recomandat

# Framework web
fastapi==0.135.1
uvicorn==0.41.0
starlette==0.52.1

# Baza de date
SQLAlchemy==2.0.48

# Validare & serializare
pydantic==2.12.5
pydantic_core==2.41.5
email-validator==2.3.0

# Autentificare & securitate
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1
cryptography==46.0.5

# HTTP & form parsing
python-multipart==0.0.22
requests==2.32.5
httpx==0.28.1

# Configurare
python-dotenv==1.2.2

# Scraping
beautifulsoup4==4.14.3
playwright==1.58.0
playwright-stealth==2.0.3
patchright==1.61.2
curl_cffi==0.15.0
vinted-scraper==3.1.1

# AI (Groq)
groq==1.1.1

# Imobiliare — detectie duplicate pe imagine (pHash + color histogram)
imagehash==4.3.2
Pillow>=11.0  # 10.x nu are wheels pentru Python 3.14

# Import / Export
openpyxl==3.1.5
reportlab==4.4.10

# Scheduler (verificare alerte)
APScheduler==3.11.2
tzlocal==5.3.1

# Rate limiting endpoint-uri de scraping manual (MODIFICARE 5)
slowapi==0.1.9

# Web Push notifications (Radar)
pywebpush==2.0.0
py-vapid==1.9.1

# ML — colectare date + predictie pret/timp vanzare (market_listings)
scikit-learn==1.9.0
joblib==1.5.3
numpy==2.4.6
pandas==3.0.3
