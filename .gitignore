# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
env/

# Node.js
node_modules/
.next/
# build static (export), generat de npm run build in frontend/
frontend/out/
.env.local

# Database
*.sql
*.db
*.db-shm
*.db-wal
# copii de backup BK-1
backend/backups/

# OS
.DS_Store
Thumbs.db

# FlipRadar — date locale sensibile (sesiuni scrapere etc.) + output health-check
backend/data/
backend/scripts/snapshot_out/
scripts/diagnostics/
# chei per-instanta generate in data dir (PKG-DATA / VAPID-AUTO)
secret_key
vapid_private_key

# PyInstaller — artefacte de build (PKG-3b), NU se comit
packaging/build/
packaging/dist/
packaging/flipradar.ico
