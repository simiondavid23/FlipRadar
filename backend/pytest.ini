[pytest]
testpaths = tests
addopts = -q
# Nota: nu setam asyncio_mode — toate testele de pornire sunt sincrone
# (TestClient e sincron). pytest-asyncio ramane in dev-deps pentru teste async viitoare.
