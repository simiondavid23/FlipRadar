-r requirements.txt

# Testare (fundatia pytest) — versiuni verificate la instalare in venv (Python 3.14).
pytest==9.1.1
pytest-asyncio==1.4.0

# Impachetare exe (PKG-3b) — build local, nu ruleaza in CI.
pyinstaller==6.21.0
