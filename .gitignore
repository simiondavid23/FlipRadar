# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
env/

# Node.js
node_modules/
.next/
# build static (export), generat de npm run build in frontend/
frontend/out/
.env.local

# Database
*.sql
*.db
*.db-shm
*.db-wal
# LAUNCH-2 — lock-ul de instanta, de langa baza
*.db.lock
# copii de backup BK-1
backend/backups/

# OS
.DS_Store
Thumbs.db

# FlipRadar — date locale sensibile (sesiuni scrapere etc.) + output health-check
backend/data/
backend/scripts/snapshot_out/
backend/scripts/audit_out/
**/scripts/diagnostics/
# migrare one-shot PostgreSQL -> SQLite (SQLITE-2), ramane locala
scripts/migrate_pg_to_sqlite.py
# chei per-instanta generate in data dir (PKG-DATA / VAPID-AUTO / FBG-2)
secret_key
vapid_private_key
cookie_encryption_key
# BNR-1: cache-ul de curs valutar persistat pe disc (in dev DATA_DIR e backend/)
backend/curs_bnr.json

# KEY-1 — cheia privata Ed25519 de semnare a licentelor (NU se comite niciodata)
scripts/licensing/keys/

# PyInstaller — artefacte de build (PKG-3b), NU se comit
packaging/build/
packaging/dist/
packaging/flipradar.ico
docs/raport_AN1_idor.md
docs/PURGE_ISTORIC_GIT.md
